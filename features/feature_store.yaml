project: spotify_intelligence
provider: local

registry:
  registry_type: sql
  path: postgresql+psycopg2://${FEAST_DB_USER}:${FEAST_DB_USER_PASSWORD}@${POSTGRES_HOST}:5432/spotify

offline_store:
  type: postgres
  host: ${POSTGRES_HOST}
  port: 5432
  database: spotify
  db_schema: feast
  user: ${FEAST_DB_USER}
  password: ${FEAST_DB_USER_PASSWORD}

online_store:
  type: redis
  connection_string: ${REDIS_HOST}:6379

entity_key_serialization_version: 2